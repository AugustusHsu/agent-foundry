---
issue: MYL-102
verdict: APPROVED
handbook_commit: 7306d1829280bb151be41cb109ae2693e16e706a
reviewer: Developer
reviewed_at: 2026-09-06
---

# 發佈審查記錄：MYL-102 手冊 `06:71` 拆開兩半的證據強度

母單 MYL-97（MYL-79／MYL-95 溢出的六項改進）的 B2。出處是 Code Reviewer 在 MYL-79
第 2 輪 APPROVED 報告 §4「次要建議 2」：`06:71` 那句把**兩種證據強度不同的宣稱合併成一句斷言**。
本單是**純文件單**，依 protocol 第 7 節分支條款不掛審查單（`SKILL.md:321`：不經 Code Reviewer
的工單由結案前的最後執行者自查，Scrum Master 巡檢兜底），本記錄由執行者 Developer 撰寫。

## 1. 變更範圍

一個檔案、一行改寫，**內容變更**（非戳記-only）：

| 檔案 | 改了什麼 |
| --- | --- |
| `06-org-structure.md`（第 71 行）| 原文「之所以留著，是因為關掉它的那支 API 是單向門——開過再關並不會回到原狀，**而能真正清乾淨的設定點只有你（在面板上）動得了**」把兩件把握程度不同的事縫成一句。改寫後拆成兩半分開陳述：**①「agent 這一側動不了」標示為已實測**（MYL-79 審查期間以 agent 身分試三支會動到成員權限的 API——改權限、改角色與授權、讀成員清單——全部回 403）；**②「在面板上動得了」標示為仍是推論**（沒有人示範過，只有使用者驗得到，在使用者實際試過之前是最合理的猜測而非已確認的事實）。同段其餘三句（實際影響為零、旗標不是平台層閘門、MYL-79 的知情保留）未動 |

| 項目 | 值 |
| --- | --- |
| 手冊 commit | `7306d1829280bb151be41cb109ae2693e16e706a` |
| 變更檔案 | `docs/handbook/06-org-structure.md` |
| 來源工單 | MYL-102（母單 MYL-97、出處 MYL-79 CR 第 2 輪報告 §4）|
| 合併 commit | `a1acbcf`（`--no-ff`）|
| 手冊差異量 | 06 章 +1／−1 行 |

**本次不走戳記旁路**：`publish-gate.sh` 的 (b) 分支只在手冊 diff 每一行都是戳記時放行，
而本次唯一那行差異是正文、不是戳記行，因此必須有這份精確匹配 `handbook_commit` 的 APPROVED 記錄。

**`handbook_commit` 指向 `7306d18` 而非合併 commit `a1acbcf`**：`git log -1 --format=%H -- docs/handbook`
在路徑過濾下套用 history simplification，`--no-ff` 合併的結果與分支側 TREESAME，因此輸出的是實際
動到手冊的那一顆。與前七次記錄的行為一致。

戳記說明：**四章戳記（`03`／`04`／`06`／`07`）本次全部不動，這是正確的。**
現況是 `03`／`04`／`07` ＝ `e62e42c`（2026-09-05）、`06` ＝ `0a0b461`（2026-09-06）。
本單**完全沒有動 `skills/foundry-protocol/SKILL.md`**（protocol 最新仍是 MYL-79 留下的 `6d6bb32`），
pre-commit 的手冊同步觸發器因此未被觸發（Passed）；`handbook-stamp` 自檢亦綠
（其判準不是「戳記等於最新 sha」，見 MYL-44 的裁定）。合併後在 main 上覆驗維持綠。

## 2. P2 前提逐項自檢

| # | 前提（MYL-23 分級表 P2） | 結果 | 證據 |
| --- | --- | --- | --- |
| 1 | 來源變更已合併進私有 main | ✅ | `git merge --no-ff myl-102-handbook-06-evidence-strength` → `a1acbcf`；`git merge-base --is-ancestor 7306d1829280bb151be41cb109ae2693e16e706a main` 回 **0**。已 `git push origin main`（`157c9de..a1acbcf`）。`git log -1 --format=%H -- docs/handbook` 回 `7306d182…`，與本欄 `handbook_commit` 逐字一致。⚠️ 首次合併時本機 main 落後 `origin/main` 兩顆（併行的 MYL-103 run 已先推 `a150493`／`157c9de`），已 `git fetch` 後 `reset --hard origin/main` 重做合併，故 `a1acbcf` 的第一父是 `157c9de` 而非過期的 `130bbd3` |
| 2 | 同步範圍僅限既定目錄 `docs/handbook/` | ✅ | `git diff --name-only 6d6bb32..a1acbcf`（`6d6bb32` ＝ 上一份已核可記錄 MYL-79 的 `handbook_commit`）共 **4 檔，其中僅 1 檔**在 `docs/handbook/` 底下（`06-org-structure.md`）。其餘 3 檔為 `docs/features/org/review-report-MYL-79.md`、`docs/publish-reviews/MYL-79.md`、`docs/standards/known-drift.md`（最後一項來自併行的 MYL-103），皆不在投影範圍——腳本只讀 `docs/handbook/` |
| 3 | 私有連結過濾輸出檢查無異常 | ✅ | `git diff 157c9de..a1acbcf -- docs/handbook/ \| grep '^+' \| grep -E 'skills/\|templates/\|docs/pilot\|docs/standards\|\.foundry\|\]\('` **無輸出**（exit 1）——本次唯一的新增行**一條 markdown 連結都沒有**，也未出現任何私有目錄路徑，過濾面完全不變。`internal-links` 自檢 70 條全綠、`anchors` 9 條全綠 |

補充：合併前在分支上跑 `make check` **exit 0**——`--selfcheck` 14 項綠
（`entry-sync`／`nav-sync`／`anchors`／`rule-ids`／`rule-marks`／`big-files`／`internal-links`／
`version-shape`／`table-shape`／`org-sync`／`handbook-stamp`／`init-copy-list`／`selfcheck-names`
十三項實際檢查通過，`mirror-recon` 因 GitHub GraphQL 觸發次級限流而回 ⏭ 跳過），
單元測試 356 項全過（200＋15＋34＋107）。
⚠️ **`mirror-recon` 的 ⏭ 不算通過**：本輪動手前它是紅的（來源端 `MYL-98`～`MYL-103` 六張漏建鏡像），
已補建並掛上看板 Status，過程另見結案留言；限流解除後需再跑一次確認。這一項與本次手冊變更無關，
不影響上表三項前提。

## 3. 公開適切性檢查

- **機敏資訊**：無。新增內容是把一句既有的自我限制揭露拆成「已驗證」與「待驗證」兩半，
  沒有引入任何新事實。無憑證、無內部網址、無個資。以 `[0-9a-f]{8}-[0-9a-f]{4}` 與
  `[0-9a-f]{8}\b` 兩式掃過本次新增行，**零命中**（沿用 `MYL-35.md`:31 的同一條判準）——
  特別確認未寫出 MYL-79 實測所涉的 agent id、member id 或 grant id。
- **內部路徑與代號**：新增內容提到工單編號 `MYL-79`（同段原本就有，非本次引入）、
  角色代稱「agent」與 HTTP 狀態碼 403。三支 API 刻意**只用中文功能描述**
  （「改權限」「改角色與授權」「讀成員清單」）而不寫出端點路徑——路徑本身不是機敏資訊，
  但對公開手冊的讀者沒有意義，且會把非技術語彙的段落拉成 API 文件。
  **未提及任何 `skills/`、`templates/`、`docs/pilot/`、`.foundry/` 路徑**（上表前提 3 的 grep 已機械確認）。
- **連結可達性**：本次未新增、未刪除、未修改任何連結，也未動任何標題，
  章節數與錨點集合皆不變 ⇒ `anchors` 綠、`nav-sync` 綠，不需要動 `mkdocs.yml`，
  `L16`（wiki 與精裝站兩套 slug 演算法）的風險面本次未被觸及。

## 4. 未通過項目

無。

## Verdict

**✅ APPROVED**
