---
issue: MYL-117
verdict: APPROVED
handbook_commit: fc666dffa7fc480001b36e11ab2d645322bd7890
reviewer: Tech Lead
reviewed_at: 2026-09-08
---

# 發佈審查記錄：MYL-117 X3 parent 全面強制：條文、頂層單例外判準與回溯補掛

承接 MYL-96 `plan` rev 1 §3「X3」（核可卡 `10db376e` 的裁定 **B1**）。主體是規則層：
protocol 第 1 節新增 `I3`（agent 開的單必須掛上位單）＋頂層單例外判準、`--selfcheck`
新增第 18 項 `issue-parent`、`docs/standards/issue-parent-backfill.md` 記 29 張回溯補掛的
逐張依據。手冊側同步一章（`06-org-structure.md`）。

**Code Reviewer 第 2 輪判 ✅ APPROVED**（報告 `docs/features/foundry-lint/review-report-MYL-117.md`
＝ commit `91127c5`，判決留言 `f775c3e7`）。本記錄由執行合併與發佈的 Tech Lead 撰寫
（P2：拍板者＝執行者本人）。

## 1. 變更範圍

| 項目 | 值 |
| --- | --- |
| 手冊 commit | `fc666dffa7fc480001b36e11ab2d645322bd7890` |
| 變更檔案 | `06-org-structure.md`（14 ＋／0 －）|
| 來源工單 | MYL-117（母單 MYL-96）|
| 合併 commit | `fc666df`（`--no-ff`）|
| 比較基準 | `7d00e30`（上一份已核可記錄 MYL-128 的 `handbook_commit`）|

逐章改了什麼：

| 章節 | ＋／－ | 改了什麼 | 性質 |
| --- | --- | --- | --- |
| `06-org-structure.md` | 14 / 0 | 「開單規則」那節新增第三條整段：`I3` 只管 agent 開的單（使用者自建的一張不動，並寫明理由是「你的單就是這棵樹的根」）、沒有母單的單為什麼是**消失**而不是報錯、29 張回溯補掛已完成並指向逐張依據、頂層單例外的寫法與「這個例外預期是空的」、例外用了會把**張數與單號印在自檢那一行的結尾**（因為例外寫在描述裡、誰都改得動，不印出來就能靜靜地讓紅字消失），以及與第二條「開單者」的對照——母單這一格改得動，所以正解是補母單，並警告不要為了變綠把單掛到不相干的母單底下 | 內容變更 |
| 其餘 7 章＋`index` | 0 / 0 | 未動 | — |

**本次不走戳記旁路**：`publish-gate.sh` 的戳記分支只在手冊 diff **每一行**都符合戳記正則時
放行，本次 14 行新增全部是實質內容、一行戳記也沒有，因此必須有這份精確匹配
`handbook_commit` 的 APPROVED 記錄。

**⚠️ `handbook_commit` 指向合併 commit `fc666df` 本身，與 MYL-128 那次不同，這是正確的。**
判準是模板寫的那一條——必須等於 `git log -1 --format=%H -- docs/handbook` 的輸出——而
不是「一律取分支側」。路徑過濾下的 history simplification 只在合併結果與**某一個 parent
TREESAME** 時才略過合併 commit；本次兩個 parent 都不 TREESAME：main 側（`05b7a61`）缺本單
改的 `06`，分支側（`91127c5`）缺 MYL-128 改的 `04`／`07`（分支基底 `7f5d276` 早於
MYL-128 合併）。⇒ 合併 commit 自己就是最後一顆動到手冊的 commit。實跑輸出即
`fc666df…`，與本欄一致。

**戳記：`06` 這一章本次不需要動戳記行，`03`／`04`／`07` 三章同樣維持不動。**
`handbook-stamp` 的判準是「戳記之後的每一顆 protocol 改動都要有**手冊任一檔**變更同行」
（覆蓋範圍刻意不是「動到對應章」，見 known-drift `GAP-6`）。本單動到 protocol 的
commit `f3574d8` 在同一顆裡就改了 `06` ⇒ 層 0 觸發器不擋、層 1 `handbook-stamp` ✅
（合併後在 main 上實跑，輸出「protocol 最新 `fc666df`」）。

## 2. P2 前提逐項自檢

| # | 前提（MYL-23 分級表 P2） | 結果 | 證據 |
| --- | --- | --- | --- |
| 1 | 來源變更已合併進 main | ✅ | 合併過程走隔離 clone（共用 workspace 由併行的 MYL-135 run 持有，`X1`）。⚠️ 第一次合併完要推時 `origin/main` 已從 `6d7d52f` 前進到 `05b7a61`（MYL-135 併入），push 被 non-fast-forward 擋下 ⇒ **重設到新的 `origin/main` 重跑同一次合併**，並逐位元組確認取回的 5 個解衝突檔與第一次的結果相同（`git diff --stat` 無輸出）、整棵樹與第一次的差異**只有 MYL-135 那三檔**。`git push origin main` → `05b7a61..bb0866f`。`git merge-base --is-ancestor fc666df… main` 與 `… origin/main` **兩條都成立**。合併後在 main 上跑 `make check`：**exit 0**、`--selfcheck` **19 項全部通過**（含合併後才並存的 `model-routing-sync` 與 `issue-parent`，兩項互不干擾）、四套單元測試 **376＋61＋34＋107** 全過 |
| 2 | 同步範圍僅限既定目錄 `docs/handbook/` | ✅ | `git diff --name-only 7d00e30..HEAD` 共 **27 檔**，其中**僅 1 檔**在 `docs/handbook/` 底下（`06-org-structure.md`）。其餘 26 檔分佈於 `skills/`、`tools/`、`docs/features/`、`docs/standards/`、`docs/publish-reviews/` 與根目錄——皆不在投影範圍，兩支腳本只讀 `docs/handbook/`。另驗此區間內動到手冊的 commit **只有本單三顆**（`f3574d8`／`90c30ff`／`fc666df`），**沒有夾帶別單尚未過審的手冊變更** |
| 3 | 私有連結過濾輸出檢查無異常 | ✅ | **實跑投影取證，不採用模板 `:39` 建議的那條 grep**（本機 `grep` 是 ugrep，該 pattern 恆回空——MYL-115 記錄 §2 已查明並在 MYL-73 造成過假綠）。`python3 tools/publish-docs/project_docs.py --src docs/handbook --out <scratch> --mkdocs mkdocs.yml --handbook-commit fc666df… --link-policy absolute` → **exit 0**、投影 **11 頁**、摘要 `3341455e…`。輸出目錄全部連結目標逐條掃描（用 python `re`，不用 grep）：**殘留的 `../`／`./` 相對路徑 0 條**——4 個指向 repo 內部的目標（`skills/foundry-protocol/SKILL.md` 12 次、`docs/pilot/pilot-log.md` 2 次、`skills/foundry-platform/config-schema.md` 與 `templates` 各 1 次）全部改寫為 `https://github.com/AugustusHsu/agent-foundry/blob/main/…` 絕對 URL。**本次新增的 14 行裡一條 markdown 連結都沒有**，過濾面未擴大 |

## 3. 公開適切性檢查

- **機敏資訊**：無。對 14 行新增內容做機械掃描（`git diff 7d00e30..HEAD -- docs/handbook`）：
  UUID、`company_id`／`project_id`／`workspace_id`／`agent_id`、`api_key`／`token`／`secret`／
  `bearer`／`password`、`http(s)://` 網址、本機路徑（`/home/`、`/Users/`、`.paperclip`）
  **五類全部 0 命中**。新增內容的性質是對讀者解釋「為什麼 agent 開的單要掛母單、為什麼你自己
  的單不受這條管、例外怎麼寫、例外用了你會怎麼知道」。
  一處刻意公開的事實：**手冊明說「之前積下來的 29 張已經補掛完畢」並指向那份逐張清單**。
  內容是團隊自身的工單整理紀錄，工單編號與標題在公開鏡像 repo 的 issue 上本來就查得到；
  該清單另做同樣的三類掃描（UUID／金鑰字樣／本機路徑）**全部 0 命中**。判為可公開。
- **內部路徑與代號**：新增行提到的 repo 路徑只有一個——反引號裡的
  `docs/standards/issue-parent-backfill.md`（**不是連結**，所以不經過改寫）。
  對外部讀者可達性已實查：repo `AugustusHsu/agent-foundry` 為 **PUBLIC**，
  `gh api …/contents/docs/standards/issue-parent-backfill.md?ref=main` 讀得到（8521 bytes）
  ⇒ 手冊站讀者照這個路徑到 repo 上找得到那份清單。體例上與第 1／8 章既有的
  「反引號寫私有路徑、讀者到 repo 查」一致（`skills/foundry-adopt/SKILL.md`、
  `skills/foundry-init/SKILL.md` 等）。其餘代號：規則代稱只用中文的「上位單」那一項與
  `make check`，**沒有出現裸的規則 ID**，段落自足。
- **連結可達性**：本次**沒有新增任何 markdown 連結**，也沒有新增章節 ⇒ 不需要動 `mkdocs.yml`。
  `internal-links` 自檢 71 條、`anchors` 9 條、`nav-sync`（8 篇）皆綠。新增段落用的是
  `###` 三級標題，未被任何既有錨點連結指向，錨點面未擴大（`X4`：agent 本機沒有渲染器，
  判準與已驗結果見 known-drift `L16`）。

## 4. 未通過項目

無。

## Verdict

**✅ APPROVED**
