---
issue: MYL-116
verdict: APPROVED
handbook_commit: d47708dbe4749084b1dfa115ef5403a98680d805
reviewer: Developer
reviewed_at: 2026-09-07
---

# 發佈審查記錄：MYL-116 X2 開單規則的兩項機械檢查：開單者白名單、PM 開單必備欄位

母單 MYL-96 `plan` rev 1 §3「X2」的落地（使用者於核可卡 `10db376e` 核可）。主體是
`tools/foundry-lint/foundry_lint.py` 的兩項新自檢（`issue-authors`／`pm-issue-fields`）
與 protocol 第 1 節 `I1`／`I2` 條文；手冊側只有 `06-org-structure.md` 一章的說明層同步。
Code Reviewer 兩輪審查後判 ✅ APPROVED（報告 `docs/features/foundry-lint/review-report-MYL-116.md`，
commit `adf271d`）。本記錄由執行合併與發佈的 Developer 撰寫。

## 1. 變更範圍

| 項目 | 值 |
| --- | --- |
| 手冊 commit | `d47708dbe4749084b1dfa115ef5403a98680d805` |
| 變更檔案 | `06-org-structure.md`（1 檔，17 ＋／0 －）|
| 來源工單 | MYL-116（母單 MYL-96）|
| 合併 commit | `810c9b2`（`--no-ff`）|
| 比較基準 | `31c5d34`（上一份已核可記錄 MYL-115 的 `handbook_commit`）|

逐章改了什麼：

| 章節 | ＋／－ | 改了什麼 | 性質 |
| --- | --- | --- | --- |
| `06-org-structure.md` | 17 / 0 | 在「Product Manager」那節後新增一節「『只有三個人能開單』是查出來的，不是擋下來的」：`I1` 白名單是**事後檢查不是閘門**（平台 21 個 permissionKey 沒有 `issues:*`）、作者欄改不了因此處置是交回 PM 覆核＋留 `I1-覆核完成：` 標記收斂紅字、平台自建單明文收容、舊單不回溯；後半交代 `I2` 四欄必填、上游欄的兩種寫法（依賴欄或描述固定行）、第五格「擋住誰」為何拿掉、以及「母單不得同時被填成 blocker」這一格反向判準 | 內容變更 |
| 其餘 7 章＋`index` | 0 / 0 | 未動 | — |

**本次不走戳記旁路**：`publish-gate.sh` 的戳記分支只在手冊 diff 每一行都符合戳記正則時放行，
本次 17 行全部是實質內容（一行戳記都沒動），因此必須有這份精確匹配 `handbook_commit` 的 APPROVED 記錄。

**`handbook_commit` 指向 `d47708d` 而非合併 commit `810c9b2`，這是正確的**：
`git log -1 --format=%H -- docs/handbook` 在路徑過濾下套用 history simplification，
`--no-ff` 合併的結果與分支側 TREESAME，因此合併 commit 不列入，輸出的是分支側最後一顆
實際動到手冊的非合併 commit。與 MYL-73／74／75／82／115 歷次行為一致。

戳記說明：**四章戳記維持 `316f06e` 不動，這不是漏做。** 本單有四顆 commit 動到 protocol
（`8f149ce`／`97d2591`／`215181f`／`d47708d`），而這四顆**每一顆都在同一顆裡一併改了
`docs/handbook/06-org-structure.md`**。`unsynced_protocol_commits()` 的判準是「戳記之後的每一顆
protocol 改動都要有手冊變更同行」，不是「戳記等於最新 sha」⇒ 層 0 觸發器不擋、層 1
`handbook-stamp` ✅（合併後在 main 上實跑，輸出「protocol 最新 `d47708d`」）。

## 2. P2 前提逐項自檢

| # | 前提（MYL-23 分級表 P2） | 結果 | 證據 |
| --- | --- | --- | --- |
| 1 | 來源變更已合併進 main | ✅ | 推送前重 `fetch --prune` 確認 `main` 與 `origin/main` 同為 `4da5d61`，且 `git merge-base main MYL-116-issue-rules-lint` ＝ `4da5d61`（分支是 main 的線性後裔，沒有落後）。`git merge --no-ff MYL-116-issue-rules-lint` → `810c9b2`；`git push origin main` → `4da5d61..810c9b2`。**合併後在 main 上**跑 `make check` **exit 0**：`--selfcheck` 17 項全綠（含本單新增的 `issue-authors`「MYL-125 起 0 張／全部 123 張」與 `pm-issue-fields`「PM 開了 0 張」），單元測試 305＋15＋34＋107 全過 |
| 2 | 同步範圍僅限既定目錄 `docs/handbook/` | ✅ | `git diff --name-only 31c5d34..HEAD` 共 **15 檔**，其中**僅 1 檔**在 `docs/handbook/` 底下（`06-org-structure.md`）。其餘 14 檔分佈於 `tools/`（3）、`skills/`（3）、`docs/`（3＝`features/` 2＋`standards/` 1）、`docs/publish-reviews/`（1，MYL-115 那份）、根目錄（4＝雙入口 2＋`Makefile`＋`.pre-commit-config.yaml`）——皆不在投影範圍，兩支腳本只讀 `docs/handbook/`。另驗此區間內動到手冊的 commit **只有本單四顆**（`8f149ce`／`97d2591`／`215181f`／`d47708d`），**沒有夾帶別單尚未過審的手冊變更** |
| 3 | 私有連結過濾輸出檢查無異常 | ✅ | **實跑投影取證，不採用模板 `:39` 建議的那條 grep**（本機 `grep` 是 ugrep，該 pattern 恆回空——MYL-115 記錄 §2 已查明並在 MYL-73 造成過假綠）。`python3 tools/publish-docs/project_docs.py --src docs/handbook --out <scratch> --mkdocs mkdocs.yml --handbook-commit d47708d… --link-policy absolute` → exit 0、投影 11 頁、摘要 `8cacf8cf…`。輸出目錄全部連結目標逐條檢查（用 python `re` 掃，不用 grep）：**沒有任何殘留的 `../`／`./` 相對路徑**——4 個指向 repo 內部的目標（`skills/foundry-protocol/SKILL.md` 12 次、`docs/pilot/pilot-log.md` 2 次、`skills/foundry-platform/config-schema.md` 與 `templates` 各 1 次）全部改寫為 `https://github.com/AugustusHsu/agent-foundry/blob/main/…` 絕對 URL，章節間連結改寫成 wiki 頁名，`#` 錨點原樣保留。`compare_projection.py` 逐章比對 **9 章全綠**。另：**本次新增的 17 行一條 markdown 連結都沒有**（`grep -c '^+.*]('` ＝ 0），連結面未擴大 |

## 3. 公開適切性檢查

- **機敏資訊**：無。對 17 行新增內容做機械掃描：UUID、`company_id`／`project_id`／`workspace_id`／
  `agent_id`、`api_key`／`token`／`secret`／`bearer`／`password`、`http(s)://` 網址、本機路徑
  （`/home/`、`/Users/`、`.paperclip`）**五類全部 0 命中**。新增內容的性質是對讀者解釋
  「開單白名單為什麼只能事後查、違規之後怎麼收斂」，以及 PM 開單四欄的讀法，無憑證、無內部網址、無個資。
- **內部路徑與代號**：新增行**沒有提到任何 `skills/`／`templates/`／`docs/`／`.foundry/` 路徑**
  （反引號內路徑掃描 0 命中），因此不存在「投影後外部讀者讀不懂」的風險。
  提到的代號只有 `make check` 與兩項檢查的中文名（`開單者白名單`／`開單必備欄位`）——
  前者是手冊既有體例，後者在同一段就說明了它做什麼。工單編號沿用本章既有體例
  （本章原本就寫著 MYL-9／12／14／23／37／59／61／73／79／96／115；本次只多出一個 `MYL-97`，
  是上游欄第二種寫法的範例字串「前置 MYL-97 已於 2026-09-05 結案」，不是指路）。
  ⚠️ 一處刻意保留的內部事實：「實查它的權限開關裡沒有任何一把管得到『開單』」——
  這是**平台能力的敘述**（對應 known-drift `L28`），不是憑證或內部網址，且是讀者理解
  「為什麼這條規則擋不住」的必要前提；措辭已避開 permissionKey 的實際字串。
- **連結可達性**：`compare_projection.py` 9 章全綠，`06-org-structure.md` 的 4 條內部連結目標相符、
  H2 節數源／wiki 皆 8、戳記存活。`internal-links` 自檢 70 條、`anchors` 9 條、`nav-sync`（8 篇）皆綠。
  **章節數不變，不需要動 `mkdocs.yml`**。`07` 章那 9 個錨點是依 GitHub slug 演算法重算的，
  本機驗不到實際渲染（`X4`：agent 本機沒有渲染器）——判準與已驗結果見 known-drift `L16`，
  本次未新增任何跨章錨點連結，風險面未擴大。

## 4. 未通過項目

無。

## Verdict

**✅ APPROVED**
