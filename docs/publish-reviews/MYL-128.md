---
issue: MYL-128
verdict: APPROVED
handbook_commit: 7d00e30161dd4f46819267b4f7ffc3689372a3eb
reviewer: CEO
reviewed_at: 2026-09-07
---

# 發佈審查記錄：MYL-128 MYL-125 C1：模型路由的條文與 schema（`M6` 常設授權、model_routing profile 欄位、五步流程分兩路徑）

母單 MYL-125 `plan` 修訂 2 第 6 節子單表的 **C1**（拆單單 MYL-127 開出）。主體是規則層：
protocol 第 8 節 `M6` 改寫為兩級常設授權＋新增供應商切換權限分級表、`config-schema.md` 的
`model_routing` 段改成「命名 profile ＋ `active` 指標」、`.foundry/config.yml` 首次寫入該段、
`foundry-model-routing` 五步流程分兩條路徑、known-drift `R6` 更新。手冊側同步兩章
（`04-decision-points.md`、`07-workflows.md`）。

**逐字條文由使用者親自核可**（確認卡 `05c99b5e-8635-4d50-90e7-1bd5c60cb964`，`human_only`，
綁本單 `clauses` 文件 rev 1，2026-09-07 13:08 accepted）——本單 AC8 明文「核可前不得合併進 main」，
合併發生在核可之後。本記錄由執行合併與發佈的 CEO 撰寫（P2：拍板者＝執行者本人）。

## 1. 變更範圍

| 項目 | 值 |
| --- | --- |
| 手冊 commit | `7d00e30161dd4f46819267b4f7ffc3689372a3eb` |
| 變更檔案 | `04-decision-points.md`（2 ＋／2 －）、`07-workflows.md`（28 ＋／6 －）|
| 來源工單 | MYL-128（母單 MYL-125）|
| 合併 commit | `65b1b92`（`--no-ff`）|
| 比較基準 | `d47708d`（上一份已核可記錄 MYL-116 的 `handbook_commit`）|

逐章改了什麼：

| 章節 | ＋／－ | 改了什麼 | 性質 |
| --- | --- | --- | --- |
| `04-decision-points.md` | 2 / 2 | 兩行：戳記 `316f06e` → `736ca80`；`H6` 那一列補上例外句——原本這一列寫得像「平台權限外的動作一律會問你」，`M6` 第 1 級一落地那句對讀者就變成假的，因此註明「同 `H4`，落在常設授權範圍內的不再逐次問」並指向第 7 章第 8 節 | 戳記＋內容變更 |
| `07-workflows.md` | 28 / 6 | 戳記 `316f06e` → `736ca80`；`M4` 那一列補上「唯一合法例外是應急配置掛明文豁免」；`M6` 那一列由「換供應商一定要問你」改寫為兩級；新增「`M6` 的兩級」整段（兩級對照表、分界判準「這個答案是不是已經答過了」、拿不準一律按第 2 級、以及第 1 級為何得以排除「一次只換一個角色」與替代的可歸因性做法）；「目前狀態」三點由「能力就位、開關沒開」改寫為「開關已經開了」＋兩套 profile ＋生效那套帶明文豁免與改回條件；章末「這對你有什麼影響」新增第 8 條的常設授權範圍與收回途徑 | 戳記＋內容變更 |
| 其餘 6 章＋`index` | 0 / 0 | 未動 | — |

**本次不走戳記旁路**：`publish-gate.sh` 的戳記分支只在手冊 diff **每一行**都符合戳記正則時放行，
本次 30 行新增裡只有 2 行是戳記、其餘 28 行是實質內容，因此必須有這份精確匹配
`handbook_commit` 的 APPROVED 記錄。

**`handbook_commit` 指向 `7d00e30` 而非合併 commit `65b1b92`，這是正確的**：
`git log -1 --format=%H -- docs/handbook` 在路徑過濾下套用 history simplification，
`--no-ff` 合併的結果與分支側 TREESAME，因此合併 commit 不列入，輸出的是分支側最後一顆
實際動到手冊的非合併 commit。與 MYL-73／74／75／82／115／116 歷次行為一致。

戳記說明：**`03`／`06` 兩章戳記維持 `316f06e` 不動，這不是漏做。**
`unsynced_protocol_commits()` 的判準是「戳記之後的每一顆 protocol 改動都要有**手冊任一檔**變更同行」
（覆蓋範圍刻意不是「動到對應章」，見 known-drift `GAP-6`），而本單唯一動到 protocol 的 commit
`736ca80` 在同一顆裡就改了 `04`、`07` 兩章 ⇒ 層 0 觸發器不擋、層 1 `handbook-stamp` ✅
（合併後在 main 上實跑，輸出「protocol 最新 `736ca80`」）。`03`／`06` 兩章本次逐項確認無需改：
第 3 章講開發流程關卡、第 6 章講組織形狀，都沒有複述 `M4`／`M6` 的敘述。

## 2. P2 前提逐項自檢

| # | 前提（MYL-23 分級表 P2） | 結果 | 證據 |
| --- | --- | --- | --- |
| 1 | 來源變更已合併進 main | ✅ | 合併前 `main`／`ws/main`／`origin/main` 三者同為 `7f5d276`，且 `git merge-base main MYL-128-model-routing-rules` ＝ `7f5d276`（分支是 main 的線性後裔，沒有落後）。`git merge --no-ff MYL-128-model-routing-rules` → `65b1b92`；`git push origin main` → `7f5d276..65b1b92`（GitHub），另 `git push ws main` 同步共用 workspace 的本地 main。`git merge-base --is-ancestor 7d00e30… main` 與 `… origin/main` **兩條都成立**。合併後在 main 上跑 `make check`：`--selfcheck` 17 項中 16 項綠、唯一紅的是 `mirror-recon` 且紅的是**本單自己的狀態格**（來源端 `in_progress`／鏡像端 `In Review`，本次心跳被喚醒時平台把狀態打回 `in_progress` 造成，收尾同步鏡像後轉綠），單元測試 305＋15＋34＋107 全過 |
| 2 | 同步範圍僅限既定目錄 `docs/handbook/` | ✅ | `git diff --name-only d47708d..HEAD` 共 **12 檔**，其中**僅 2 檔**在 `docs/handbook/` 底下（`04-decision-points.md`、`07-workflows.md`）。其餘 10 檔分佈於 `skills/`（4）、`docs/`（3＝`features/` 1＋`standards/` 1＋`publish-reviews/` 1，後兩者含 MYL-116 那份記錄）、根目錄（3＝雙入口 2＋`.foundry/config.yml`）——皆不在投影範圍，兩支腳本只讀 `docs/handbook/`。另驗此區間內動到手冊的 commit **只有本單兩顆**（`736ca80`／`7d00e30`），**沒有夾帶別單尚未過審的手冊變更** |
| 3 | 私有連結過濾輸出檢查無異常 | ✅ | **實跑投影取證，不採用模板 `:39` 建議的那條 grep**（本機 `grep` 是 ugrep，該 pattern 恆回空——MYL-115 記錄 §2 已查明並在 MYL-73 造成過假綠）。`python3 tools/publish-docs/project_docs.py --src docs/handbook --out <scratch> --mkdocs mkdocs.yml --handbook-commit 7d00e30… --link-policy absolute` → exit 0、投影 **11 頁**、摘要 `065c53b4…`。輸出目錄全部連結目標逐條掃描（用 python `re`，不用 grep）：**沒有任何殘留的 `../`／`./` 相對路徑**——4 個指向 repo 內部的目標（`skills/foundry-protocol/SKILL.md` 12 次、`docs/pilot/pilot-log.md` 2 次、`skills/foundry-platform/config-schema.md` 與 `templates` 各 1 次）全部改寫為 `https://github.com/AugustusHsu/agent-foundry/blob/main/…` 絕對 URL，章節間連結改寫成 wiki 頁名，`#` 錨點原樣保留。`compare_projection.py` 逐章比對 **9 章全綠** |

## 3. 公開適切性檢查

- **機敏資訊**：無。對 30 行新增內容做機械掃描（`git diff d47708d..HEAD -- docs/handbook`）：UUID、
  `company_id`／`project_id`／`workspace_id`／`agent_id`、`api_key`／`token`／`secret`／`bearer`／`password`、
  `http(s)://` 網址、本機路徑（`/home/`、`/Users/`、`.paperclip`）**五類全部 0 命中**。
  新增內容的性質是對讀者解釋「換供應商為什麼分兩級、哪一級不會再問你、目前生效的配置帶著什麼豁免」。
  ⚠️ 一處刻意公開的事實：**手冊明說「Developer 與 Code Reviewer 現在跑在 Codex 上」以及
  `codex-emergency` 暫時放棄 `M4` 保護**。這是本單條文的核心設計——「豁免要被看見，才不會靜靜變成
  新的預設」——寫在私有側等於自我抵消。內容是團隊自身的工具選擇與流程取捨，不是憑證、不是客戶資訊，
  且 profile 名稱與角色名在 `.foundry/org.yml`／公開 repo 裡本來就查得到。判為可公開。
- **內部路徑與代號**：新增行**沒有提到任何 `skills/`／`templates/`／`docs/pilot/`／`.foundry/` 路徑**
  （反引號內路徑掃描 0 命中）。提到的代號有三類，對外部讀者都在同段內自足：規則 ID（`M4`／`M5`／`M6`／`H4`／`H6`——
  第 4 章有「那些 `H1`、`H2` 是什麼」專段解釋編號制度）、profile 名（`codex-emergency`／`normal-mixed`——
  同段落就寫了各自是什麼）、工單編號（`MYL-36`／`MYL-125`／`MYL-128`，沿用本章既有體例）。
  `python3 tools/model-routing/probe_providers.py` 那行指令是本章原有內容，本次未動。
- **連結可達性**：`compare_projection.py` 9 章全綠。本次**只新增 1 條 markdown 連結**：
  第 4 章 `H6` 列裡的 `[第 7 章](07-workflows.md)`，投影後改寫為 wiki 頁名 `07-workflows`
  （已在投影輸出的連結目標統計中確認）。`internal-links` 自檢 71 條、`anchors` 9 條、
  `nav-sync`（8 篇）皆綠。**章節數不變，不需要動 `mkdocs.yml`**。第 7 章那 9 個錨點是依 GitHub slug
  演算法重算的，本機驗不到實際渲染（`X4`：agent 本機沒有渲染器）——判準與已驗結果見 known-drift `L16`，
  本次未新增任何跨章錨點連結，風險面未擴大。

## 4. 未通過項目

無。

## Verdict

**✅ APPROVED**
