---
issue: MYL-121
verdict: APPROVED
handbook_commit: df08d1a9c34e8f920d0248596fea6318141c7854
reviewer: Tech Lead
reviewed_at: 2026-09-08
---

# 發佈審查記錄：MYL-121 規範同步：handbook-version-tags 永久 Disabled 之後，七處敘述與現況不符

使用者於 2026-09-06 在 MYL-61 的卡 `10279852` Q2 選了 `permanent_off`（「永久 Disabled ——
我不想每次都切」），並於 **2026-09-08 09:47:33 +08:00** 實際把 ruleset 切下去。那一刻起，
規範側七處「推 tag 前切 Disabled、推完切回 Active」的敘述全部過期。主體是規則層
（protocol 第 7 節第五步、`V1` 例外、`V3` 違反段、known-drift `L22` 補第二次裁定），
手冊側同步三章（`03`／`04`／`07`）。

**Code Reviewer 第 1 輪判 ✅ APPROVED**（報告 `docs/features/git-flow/review-report-MYL-121.md`
＝ commit `0a3fa9b`，判決留言 `ed4e4ca8`）。本記錄由執行合併與發佈的 Tech Lead 撰寫
（P2：拍板者＝執行者本人）。

## 1. 變更範圍

| 項目 | 值 |
| --- | --- |
| 手冊 commit | `df08d1a9c34e8f920d0248596fea6318141c7854` |
| 變更檔案 | `03-workflow.md`（1 ＋／1 －）、`04-decision-points.md`（2 ＋／2 －）、`07-workflows.md`（1 ＋／1 －）|
| 來源工單 | MYL-121（母單 MYL-61）|
| 合併 commit | `f61a1c0`（`--no-ff`）|
| 比較基準 | `702216e`（上一份已核可記錄 MYL-139 的 `handbook_commit`）|

逐章改了什麼：

| 章節 | ＋／－ | 改了什麼 | 性質 |
| --- | --- | --- | --- |
| `03-workflow.md` | 1 / 1 | 第四步後的「第五步不是結案條件」那段，句尾從「推 tag 前要先把 `handbook-version-tags` 切成 Disabled、推完切回 Active」改成「**推 tag 前不必再解鎖**——那條規則已於 2026-09-08 依你的選擇永久關閉」，並把代價（誤推保護沒了）指向第 4 章 | 內容變更 |
| `04-decision-points.md` | 2 / 2 | 兩段。①「手冊發一版也在這一關」：刪掉 Settings → Rules → Rulesets 的解鎖路徑，改寫成「不必再解鎖」，並展開**換掉的是什麼**——那條規則從來不是「只有你能打 tag」（共用同一個 GitHub 身分，規則分不出誰是誰），它買到的是**防誤推**與**留痕**兩件事，現在兩件都沒了；點明誤推的後果已從「被 `GH013` 拒收」變成「直接推成功並觸發建站，而版本號受『不重打』保護、改不掉」。②「你也可以直接叫 agent 動 tag」：三項義務改兩項，明寫「動完提醒你切回 Active」**已取消**，同時保留並反轉禁令的方向——切開關仍只有使用者能做，agent 不代勞，管的是「哪天你重新啟用了它，agent 不會替你關掉」 | 內容變更（含義務取消）|
| `07-workflows.md` | 1 / 1 | 第 6 節「打 tag 前要先解鎖」整條改寫為「不必解鎖，但也不再有任何保護」，用 ①②③ 行內標號列出少掉的三樣：防誤推、留痕、以及「已發佈的 tag 刪掉也建不回同名」這個附帶效果。保留原有的裁定「這條規則本來就分不出誰在推 ⇒ 規則文字上一直都算自律」，差別寫成「從前至少有一塊速度墊，現在連那塊也關了」 | 內容變更 |
| 其餘 5 章＋`index` | 0 / 0 | 未動 | — |

**本次不走戳記旁路**：`publish-gate.sh` 的戳記分支只在手冊 diff **每一行**都符合戳記正則時
放行，本次 4 行新增全部是實質內容、一行戳記也沒有，因此必須有這份精確匹配
`handbook_commit` 的 APPROVED 記錄。

**⚠️ `handbook_commit` 指向分支側的 `df08d1a` 而不是合併 commit `f61a1c0`。** 判準是模板寫的
那一條——必須等於 `git log -1 --format=%H -- docs/handbook` 的輸出——**每次實跑，不要照抄
上一份的形狀**（MYL-117 那次答案是合併 commit 自己，MYL-139 與本次是分支側，兩個方向都出現過）。
本次實測：合併結果對分支側 parent（`0a3fa9b`）就 `docs/handbook` **TREESAME**（main 側自
`702216e` 起沒再動過手冊），對 parent1 則不 TREESAME ⇒ 路徑過濾下的 history simplification
略過合併 commit，最後一顆動到手冊的是 `df08d1a`。實跑輸出即 `df08d1a9c34e…`，與本欄一致。

**戳記：四章戳記行本次全部不動（`03`／`06` 維持 `316f06e`、`04`／`07` 維持 `736ca80`），
這是判斷不是疏漏。** 兩層理由：

1. **機械層不要求**。`handbook-stamp` 的判準是「戳記之後每一顆改 protocol 的 commit 都要有
   **手冊任一檔**變更同行」（`unsynced_protocol_commits()` docstring 明寫），**不是**「戳記
   等於 protocol 最新 sha」。本單動到 protocol 的 `df08d1a` 在同一顆裡就改了 `03`／`04`／`07`
   ⇒ 層 0 觸發器不擋、層 1 `handbook-stamp` ✅（合併後在 main 上實跑，輸出「protocol 最新
   `df08d1a`」）。**Code Reviewer 已對這一格做過反向突變**：把手冊變更從同一顆 commit 移除
   → `❌ handbook-stamp`、rc=1，對照組綠 ⇒ 這個綠燈不是空過的。
2. **推戳記是一句宣稱，本輪撐不起那句話**。戳記的語意是「這一章我逐章對照過 protocol」。
   本輪對照的只有 ruleset 那一條線（第 7 節第五步、`V1`、`V3`），三章的其餘內容一行也沒重讀。
   推它們就正好是 protocol `:402` 點名的那一格：**戳記推了、內容其實沒對照過**——機械層擋不住，
   只會讓下一個人以為那三章是新鮮的。沿用 MYL-139 同一判準。

## 2. P2 前提逐項自檢

| # | 前提（MYL-23 分級表 P2） | 結果 | 證據 |
| --- | --- | --- | --- |
| 1 | 來源變更已合併進私有 main | ✅ | 合併前 `git fetch origin`，確認本地 `main` 與 `origin/main` **雙向 `--is-ancestor` 都成立**（同在 `ae439be`，排除 MYL-136 那次「GitHub main 領先本地」的形態）。`git merge --no-ff` → `f61a1c0`，輸出 `Merge made by the 'ort' strategy`、6 檔異動——**是真的合併，不是「Already up to date」的假綠**。`git push origin main` → `ae439be..f61a1c0`，推後 `git rev-parse main origin/main` **兩者同值**。`git merge-base --is-ancestor df08d1a… main` 與 `… origin/main` **兩條都成立**。合併後在 main 上跑 `make check`：**exit 0**（以 `set -o pipefail` 取真 rc，不讓管線吃掉）、`--selfcheck` **19 項全部通過**、四套單元測試 **376＋84＋34＋107** 全過 |
| 2 | 同步範圍僅限既定目錄 `docs/handbook/` | ✅ | `git diff --name-only 702216e..HEAD` 共 **8 檔**，其中**僅 3 檔**在 `docs/handbook/` 底下（`03`／`04`／`07`）。其餘 5 檔分佈於 `docs/features/`、`docs/publish-reviews/`、`docs/standards/`、`skills/`——皆不在投影範圍，兩支腳本只讀 `docs/handbook/`。另驗此區間內動到手冊的 commit **只有本單一顆**（`df08d1a`）⇒ **沒有夾帶別單尚未過審的手冊變更**（區間內另有 MYL-139 的記錄 commit，它沒再碰手冊）|
| 3 | 私有連結過濾輸出檢查無異常 | ✅ | **實跑投影取證，不採用模板 `:39` 建議的那條 grep**（本機 `grep` 是 ugrep，該 pattern 恆回空——MYL-115 記錄 §2 已查明並在 MYL-73 造成過假綠）。`python3 tools/publish-docs/project_docs.py --src docs/handbook --out <scratch> --mkdocs mkdocs.yml --handbook-commit df08d1a… --link-policy absolute` → **exit 0**、投影 **11 頁**、摘要 `469a03741edd`。輸出目錄全部連結目標逐條掃描（用 python `re`，不用 grep）：**殘留的 `../`／`./` 相對路徑 0 條**——指向 repo 內部的目標（`skills/foundry-protocol/SKILL.md` 12 次、`docs/pilot/pilot-log.md` 2 次、`skills/foundry-platform/config-schema.md`／`templates`／repo 根各 1 次）全部改寫為 `https://github.com/AugustusHsu/agent-foundry/…` 絕對 URL。**本次新增 4 行裡的 6 條連結全部是手冊內章節互指**（`07-workflows.md` ×3、`04-decision-points.md` ×3），投影後成為 wiki 頁名形式（`07-workflows`、`04-decision-points`，無副檔名）⇒ **過濾面未擴大，沒有任何新的私有路徑進入公開面** |

## 3. 公開適切性檢查

- **機敏資訊**：無。對 4 行新增內容做五類機械掃描（`git diff 702216e..HEAD -- docs/handbook`
  取 `+` 行）：UUID、`company_id`／`project_id`／`workspace_id`／`agent_id`／`issue_id`、
  `http(s)://` 網址、本機路徑（`/home/`、`/Users/`、`.paperclip`）**四類全部 0 命中**；憑證字樣
  1 命中，逐字調閱上下文為「（agent 的 **token** 改得動它）」——是**能力陳述**（說明這條禁令
  沒有機械後盾、只能靠自律），**不是任何憑證值**。判為可公開。

  **本次值得單獨判的一件事：把「保護關掉了」寫上公開站，安不安全？** 判為安全，理由是那條
  ruleset **從來就不是對外的安全邊界**——它擋的是所有人（含使用者本人），因為此 repo 中
  使用者與所有 agent 共用同一個 GitHub 身分，規則以 actor 為單位而分不出誰是誰（`L22` 的推導，
  本次未推翻、仍留著）。真正擋住外部人的是 repo 的**寫入權限**，那一層沒有任何變動。所以
  公開「速度墊已關」不會讓任何原本推不了 tag 的人變成推得了，攻擊面未擴大。反過來說，
  **AC2 明文要求「誠實記錄失去了什麼、寫明而不是靜靜刪掉」**——把代價藏起來才是違反本單。

- **內部路徑與代號**：新增行提到的 repo 路徑只有反引號裡的 `handbook-version-tags`（規則名，
  不是路徑）與 `.foundry/config.yml`（原有文字未動）。**沒有新增任何指向 `skills/`、`templates/`、
  `docs/pilot/` 的連結**。對外部讀者的可讀性：`GH013 … creations being restricted` 是 GitHub
  的公開錯誤碼、`Settings → Rules → Rulesets` 是公開 UI 路徑，兩者都不需要前導知識；
  「MYL-61 的卡」屬工單編號，體例上與手冊既有敘述一致（第 3／4／7 章多處已如此引用）。
- **連結可達性**：本次**沒有新增章節**⇒ 不需要動 `mkdocs.yml`（`nav-sync` 8 篇綠）。6 條連結
  全部是既有章節互指，目標章節（`04`／`07`）都在 nav 內、投影後頁名對得上；`internal-links`
  自檢 **142 條**綠、`anchors` **9 條**綠。改寫全部發生在既有標題底下，**沒有動任何標題文字**
  ⇒ 錨點面未變、既有指向它們的連結不受影響（agent 本機沒有渲染器，判準與已驗結果見
  known-drift `L16`）。

## 4. 未通過項目

無。

## Verdict

**✅ APPROVED**
