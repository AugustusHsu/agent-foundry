# 審查報告：MYL-100 `test_權限值域外被擋下` 收成一條參數化測試

| 欄位 | 值 |
| --- | --- |
| 工單 | MYL-100（審查單 MYL-106） |
| 分支 | `feat/MYL-100-permission-domain-test` @ `1e318f1`（本報告 commit 後 +1） |
| 審查範圍 | 第 2 輪。`21e4e67..1e318f1` 共 1 顆 commit、1 檔、+17/-8 |
| 審查者 | Code Reviewer |
| 日期 | 2026-09-06 |

第 1 輪 ❌ CHANGES REQUESTED（兩項瑕疵），本輪**只對那兩項逐項驗**，不重新全面審查。
審查在 `git clone --shared` 的隔離副本進行（共用 workspace 由 MYL-80 的 run 佔著，`X1`）。

## 0. 機械層（第 1 層）

| 指令 | 結果 |
| --- | --- |
| `git ls-remote` 對遠端 tip | `1e318f1a35ab8f972f68d1df7666c0e5fb023064`，與工單宣告一致 |
| `git diff --name-only 130bbd3..1e318f1` | 只有 `tools/foundry-lint/test_foundry_lint.py`，全分支未夾帶別單 |
| `git log --oneline` | `1e318f1 🐛 MYL-100 訊息斷言只看「值域是」前半段…`＝gitmoji ＋繁中標題 |
| `make check`（分支上） | exit 0；13 項自檢通過、`mirror-recon` ⏭（見 §7）；199＋15＋34＋107 tests OK |
| 試合併現行 main（`c3970cc`，非分支基底 `130bbd3`） | 零衝突。合併後 `make check` 仍 exit 0、14 項自檢中 13 ✅ 1 ⏭、**215**＋15＋34＋107 tests OK。**刻意沒推**，合併時點依 protocol 第 7 節在 APPROVED 之後 |

## 1. AC 逐條核對

| AC | 結果 | 證據（皆為我自跑，非引用交付回報） |
| --- | --- | --- |
| 1. `grep -rn 'def test_權限值域外被擋下' tools/foundry-lint` 只剩一處定義 | ✅ | `test_foundry_lint.py:1220`，一處。另掃全 repo `--include=*.py` 找同名與舊名 `值域外的值擋下`，同樣只有這一處 |
| 2. 收斂後案例數不少於收斂前所有重複測試的案例總和 | ✅ | 收斂前 2 條各 1 例＝2；收斂後 `OUT_OF_DOMAIN_PERMISSIONS` 4 例。我照交付回報那條指令原樣重跑，得 `4 ('不在值域裡的權限', 'do_anything', 'create_skill', 'CREATE_SKILLS')`——**這輪的證據重跑得出來**（第 1 輪那句不成立的已作廢，見 §3） |
| 3. `python3 -m unittest discover tools/foundry-lint` 全數通過 | ✅ | `Ran 199 tests in 11.775s OK`；合併現行 main 後 `Ran 215 tests OK` |

## 2. 四維檢查

- **正確性**：修正後的斷言 ``any("`%s`" % bogus in f.split("值域是")[0] and "值域" in f …)`` 有**兩層**防護，而且兩層各自都擋得住第 1 輪那個失效模式：①犯規值帶反引號比對，而訊息尾巴的值域列舉是裸值（`assign_tasks｜…｜create_skills`），近似值吃不進去；②`split("值域是")[0]` 把列舉整段排除在比對範圍外。四個案例的鑑別力我逐例驗過（§3 的 E1／E2）。
- **規格符合度**：本單的規格是收掉「反例靜默退化」。第 1 輪引入的同型退化（斷言喪失鑑別力）已消除，且 docstring 明寫「別放寬回整句子字串比對」及原因——規格意圖有被寫進程式碼旁邊，不只在工單裡。
- **安全性**：無發現。純測試檔，無輸入驗證面、無注入面、無機敏資料落地。
- **可維護性**：守著鑑別力的那個突變寫進 docstring（次要建議 2），常數搬到類別頂部與本檔另外三個類別常數一致（次要建議 1）。第 1 輪指出的兩點都落地。

## 3. 重大瑕疵清單

**本輪無新增瑕疵。** 第 1 輪兩項的處置與我的獨立覆驗：

| 第 1 輪瑕疵 | 處置 | 我的獨立覆驗 |
| --- | --- | --- |
| 1. `:1246` 的 `any(bogus in f …)` 是整句子字串比對，`create_skill` 被值域列舉裡的 `create_skills` 吃掉 ⇒ 該例第二道斷言恆真 | 已修（`:1253`），只在「值域是」之前那半段找帶反引號的犯規值 | 四個突變實驗，見下表。**已修復** |
| 2. 交付回報「`-v` 逐例列出四個 subTest」不成立 | 已訂正，換成常數列舉 ＋ 突變下的 `failures=4` | 兩項證據我都原樣重跑得出來（AC2 那列、E1）。**已訂正** |

四個突變實驗（E1／E2 是驗交付回報的宣稱，E3／E4 是我另外設計的，交付回報沒跑過）：

| # | 突變 | 預期 | 實測 |
| --- | --- | --- | --- |
| E0 | 無（原樣） | 全綠 | `Ran 199 tests OK` |
| E1 | 訊息 `` 有 `{perm}` `` → `` 有 `某個值` ``（不指名犯規值）＋**新**斷言 | 四例全紅 | `FAILED (failures=4)`，逐例列出 `不在值域裡的權限`／`do_anything`／`create_skill`／`CREATE_SKILLS` |
| E2 | 同一突變 ＋ **舊**斷言（`git show 21e4e67:` 取回原檔） | 三例紅、`create_skill` 漏網 | `FAILED (failures=3)`，逐例列出 `不在值域裡的權限`／`do_anything`／`CREATE_SKILLS` |
| E3 | 犯規值改到「值域是 …」**之後**才指名（仍帶反引號） | 四例全紅（證明 `split` 那層真的在做事，不是只靠反引號） | `FAILED (failures=4)` |
| E4 | 保留指名、把「值域是」改成「值域清單」（`split` 退化成整句） | 全綠（訊息仍正確指名，不該被誤殺） | `OK` |

⚠️ **E1／E2 的重點不是 4 與 3 這兩個數字，是差集。** 計數相同不蘊含差集相同，所以我抓的是逐例名稱：E1 ∖ E2 ＝ `{create_skill}`，正好是第 1 輪指出的那一例。交付回報宣稱的「3 → 4 的差就是 `create_skill`」成立。
每個突變腳本都自帶 `assert s2 != s`，排除 `replace` 退化成 no-op 的假陰性；跑完還原並以 `git status` 驗回 0 個改動。

## 4. 次要建議

| # | 第 1 輪建議 | 本輪結果 |
| --- | --- | --- |
| 1 | 常數搬到類別頂部 | 已採納，`:1170`（類別 docstring 之後），與 `ENABLED`／`ISSUE`／`TABLE` 一致 |
| 2 | 把守著鑑別力的突變寫進 docstring | 已採納，並多寫了「別放寬回整句子字串比對」的禁令與原因 |
| 3 | §5 三題 | 開發者無異議，照第 1 輪的判定 |

本輪新增一項觀察，**不要求修改**：E4 顯示這條斷言對「訊息改寫但仍正確指名犯規值」是容忍的（不會誤殺），E3 顯示對「指名位置移到值域列舉之後」會轉紅。後者等於把訊息的**語序**也釘住了——這是刻意的收緊（docstring 已載明），日後若要改動訊息語序，記得那不是壞掉，是這條測試在盡責。

## 5. 分支收尾檢查

| 項目 | 結果 |
| --- | --- |
| 一單一分支 | ✅ 全分支 2 顆 commit 都只動 `tools/foundry-lint/test_foundry_lint.py` |
| commit 訊息 | ✅ 兩顆皆 gitmoji ＋繁體中文標題，正文寫明修的是哪一項瑕疵 |
| 與現行 main 合併 | ✅ 零衝突，合併後全綠（§0） |
| 手冊同步義務 | 不適用：未動 `skills/foundry-protocol/`，`handbook-stamp` ✅ |
| 發佈四步 | 不適用：未動 `docs/handbook/` |

## 6. 附帶裁定：`S5` 的證據強度（開發者在 MYL-106 提問）

開發者在轉 `blocked` 時實測了 `unblockDescriptor.owner` 的三種形狀，問要不要把 `known-drift` 的 `S5` 升級證據強度、要不要開單。裁定如下，**理由不是「多一個人看過」**：

1. `known-drift.md` 沒有「證據強度」這個欄位，也沒有單人／雙人觀測的標記慣例——`L27` 的前例是把重跑結果寫成條目裡的一句括號註記。所以「升級強度」這個動作在本檔沒有對應物。
2. 但覆驗過程**撞出了 `S5` 真正的缺口**，這才是值得記的：`S5` 只寫了「`unblockDescriptor` 要 `blocked` 才收」這一個方向，沒寫另一個方向的 422——`{"status":"blocked"}` 不帶理由會被擋，而錯誤訊息列舉了**三種**合法理由（未解 blocker／pending interaction 或 approval／`unblockDescriptor`），其中「pending interaction 或 approval」`S5` 完全沒提。兩個 422 合起來是雞生蛋，正解是**同一包 PATCH 一起送**，這條操作知識目前不在文件裡。
3. 兩個 422 我本輪都自己打出來過（回應碼、訊息逐字、403/422 之後回頭 `GET` 確認整包不生效），不是只採信交付回報。

⇒ **開一張單補 `S5`**（見 MYL-108），不搭 MYL-103（已於 `157c9de` 結案）。屬純文件改動，依 protocol 免走 CR、`known-drift.md` 免走發佈四步。

## 7. `mirror-recon`：本輪仍覆驗不到

隔離 clone 接上真 GitHub remote 後跑，拿到的是 ⏭：`讀不到鏡像端：gh issue list 失敗：GraphQL: API rate limit already exceeded`。
本輪另外用真呼叫確認了額度的形狀：**REST 面可用**（`gh api repos/…` 回得了值），**GraphQL 面仍滿**。

**⏭ 是「跳過未檢查」，不是綠燈**，所以第 1 輪那兩項紅燈歸屬（MYL-104 一對多、MYL-103 狀態不同步）本輪仍無獨立證據，我不主張它已解決。該項檢的是板面狀態、與本分支內容（單一測試檔）無關，不影響本 Verdict。

## Verdict

**✅ APPROVED**

第 1 輪的兩項逐項驗過：瑕疵 1 已修且鑑別力經四個突變證成（含差集逐例比對），瑕疵 2 的證據換成重跑得出來的形式。兩項次要建議都採納。合併現行 main 零衝突、合併後全綠。
