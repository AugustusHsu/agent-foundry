# 審查報告：MYL-75 T3 視覺能力邊界：`foundry-browser` 增訂「唯讀檢視模式」

| 欄位 | 值 |
| --- | --- |
| 工單 | MYL-75 |
| 分支 | `feat/MYL-75-readonly-view-mode` ＠ `d0464dc` |
| 審查範圍 | 兩輪合併定稿。第一輪 `main`(`e006700`)..`e2902aa`；本輪複審 `e2902aa`..`d0464dc`（5 檔 +17/-9）。分支全幅 `origin/main`...`d0464dc`＝6 檔 +69/-7 |
| 審查者 | Code Reviewer |
| 日期 | 2026-09-06 |

第一輪 Verdict ❌ CHANGES REQUESTED（留言 `12c412e7`），兩項重大瑕疵。Tech Lead 於 `d0464dc`
逐項修完，本輪對著上一輪清單逐項複驗，**不重新全面審查**（角色 skill「複審」條）。

兩輪均在 `$PAPERCLIP_RUN_SCRATCH_DIR` 的**隔離 clone**（origin 改指真 GitHub remote、非淺 clone）
執行，避開共用 workspace 讀到別的 run 半成品（`X1`／MYL-60）。

## 0. 第 1 層機械檢查（全過）

```
make check                              → exit 0；自檢 10 項全綠、288 測試全過（132+15+34+107）
--selfcheck                             → 已登記 ID 43 個（未增減）、mirror-recon 綠（來源 27／鏡像 17）
git diff --stat origin/main...d0464dc   → 6 檔全屬本單，無夾帶
git log --oneline origin/main..d0464dc  → e2902aa ✨…／d0464dc 🐛…（均為 gitmoji ＋繁中標題）
```

`handbook-stamp` 綠、04 章戳記停在 `e62e42c` **仍然是對的**：判準是
「戳記之後每顆動 protocol 的 commit 都要有手冊變更同行」（`foundry_lint.py:753-776`），
protocol 與手冊同顆 commit 即算已同步，不必補戳記——與 MYL-71 先例一致。

## 1. AC 逐條核對

| AC | 結果 | 證據 |
| --- | --- | --- |
| AC1 新增「唯讀檢視模式」一節，A＝等級邊界＋C＝效力邊界 | ✅ | `foundry-browser`:195-228（表格 :202-203 分列等級／效力）；規則本體 protocol:238-247。**第一輪的範圍過寬已修**，見第 3 節瑕疵 2 |
| AC2 明寫機械事實＋為什麼不用 tool-profile | ✅ | 機械事實 :210-212；tool-profile 兩理由 :216-220。**第一輪判 ❌ 的理由 2 已改寫**，見第 3 節瑕疵 1 |
| AC3 FV skill 補「唯讀檢視 ≠ 驗證」分界 | ✅ | `roles/frontend-verifier`:65-67，含「前端唯一有效力的證據是你依上面格式產出的報告」。本輪未動，且其原文即寫「別的角色（CEO／PM）」——與收斂後的範圍一致，無連帶修改需求 |
| AC4 `role-ceo`／`role-pm` 各一行指過去、不複製 | ✅ | `roles/ceo`:28、:35、:57；`roles/pm`:57。全 repo grep `` `F1` ``／`` `F2` `` 共 6 處引用點，判準字面（`L1`、不互動、不得當證據）只存在於 protocol 與 `foundry-browser`，角色 skill 內無第二份規範 |
| AC5 第 11 節登記 ID ＋標對【機械】／【自律】 | ✅ | 索引 protocol:648；定義在**第 4 節**（`### 唯讀檢視模式` ＠ :238，第 5 節起於 :249）；`【自律】` ＠ :247，`rule-marks` 綠。本輪未新增規則、未動 ID 與標記 |
| AC6 `--selfcheck` 全綠、`make check` 過、動到手冊走發佈四步 | ✅ | 見第 0 節。發佈四步屬合併者，交接事項見第 5 節 |

## 2. 四維檢查

- **正確性**：瑕疵 1 已解。新增文字裡的三個機械宣稱我逐項對原始碼／原文複驗，全部屬實
  （`L1` 判定訊號＝瀏覽器二進位不含 MCP ＠ `foundry-browser`:38；`compute_level()` 無可用 MCP
  即回 `1` ＠ `probe_browser.py:268-275`；`LEVEL_LABEL[1]` 明載可截圖 ＠ :104）。
  Lighthouse 改列「超過 `L1` 的動作」亦與 §2 分級表（:40 `L3`）相符。
- **規格符合度**：瑕疵 2 已解。`F1` 適用範圍在**六處**全部一致講 CEO／PM：protocol:242（規則本體）、
  protocol:247（違反段）、`foundry-browser`:198、`roles/frontend-verifier`:65、
  `docs/handbook/04`:64-66、`roles/ceo`:35／`roles/pm`:57（角色自述，範圍隱含）。
  與 AC1 字面及裁定卡 `f80e66b3` Q3 的授權範圍相符。
- **安全性**：無發現。**我在第一輪提的安全論點（`F1` 應涵蓋 Developer）被 Tech Lead 不採納，
  我獨立複驗後接受該反駁**——但把真正在做事的那條寫清楚，以免日後重啟討論時再繞一圈：
  role-agnostic 的保護有兩層，一是 `.mcp.json` 對兩個 server 都寫死 `--isolated`（我實讀該檔確認），
  二是 `foundry-browser`:192「對正式站只做唯讀瀏覽；任何破壞性操作依 `H5` 升級」——**第二條才是
  直接管住「非驗證角色在正式站點下去」的那一句**，而不是 §6 第一條「兩套 profile 硬分離」
  （它管的是驗證與日常任務不得共用 profile，不是禁止互動）。結論不變：`F1` 不必兼任通用安全條款。
- **可維護性**：次要建議 1（節號→標題引用）已採納，`roles/ceo`:28 與 `roles/pm`:57 改為
  「`foundry-browser`「唯讀檢視模式」一節」，與 protocol:245 用語一致——節號正是本單挪動過的東西，
  這個改法是對的。§7 插入後的節號順延，全 repo 對本檔的散文引用（§2.1／§4／§6／§2.4 共 5 處）
  無一指向舊 §7／§8，零連帶修改。

## 3. 重大瑕疵清單（第一輪）逐項複驗

| # | 第一輪判定 | 本輪結果 |
| --- | --- | --- |
| 1 | `foundry-browser`:217-218 §7.1 理由 2 的機械宣稱被本 repo 自己推翻 | ✅ **已解** |
| 2 | protocol:242 `F1` 適用範圍與其餘三處、與 AC1 字面都不同 | ✅ **已解** |

**瑕疵 1** — 理由 2 改談 tool-profile 真正的粒度問題（要逐工具維護放行清單、清單隨 MCP server
改版漂移），標題（粒度是「工具」）與內文終於講同一件事，且不再與 §2 打架。另補一段警語
（:222-226）擋住「拿掉 server 就等於 `F1`」的錯誤推論——坦承拿掉 MCP 確實會落到 `L1`，
但那條路切錯維度（`.mcp.json` 是情境級，拿掉會連 Frontend Verifier 的 `L2` 一起拿掉）。
處置正確：把反證收編成警語，比刪掉宣稱更能防住下一個讀者。

⚠️ **Tech Lead 多修了一處，是對的，且那一處是我第一輪漏看的**：同一則被推翻的宣稱在
**protocol 違反段（:247）也有一份**——只修 `foundry-browser` 會把第二份留在規則本體那一份上。
我本輪 grep `出自同一個`／`同一個 MCP`／`連截圖`／`截圖與點擊` 全 repo（排除 `site/`）確認**零殘留**。
教訓歸我：訂正機械宣稱要先 grep 全 repo 找副本，不能只修報告指出的那一行。

**瑕疵 2** — 選 (a) 收斂為 CEO／PM。我接受這個選擇：硬規則的範圍擴張應是明示決策，
而授權面（AC1 與裁定卡）只講 CEO／PM。protocol:242 末句明寫「本條是**列舉、不是通則**」
並點名 Developer 本機自測不受約束，把「收斂」與「疏漏」區分開來——這一句是必要的，
少了它下一個讀者會把收斂讀成漏寫。`foundry-browser`:198 的「見 protocol `F1` 末句」
指向準確（該句確為 :242 的最後一句）。

## 4. 次要建議

第一輪四項的處置我全部接受，其中兩項仍開著，記在這裡以免遺失：

| # | 狀態 |
| --- | --- |
| 1 節號→標題引用 | ✅ 已採納 |
| 2 Lighthouse 分類 | ✅ 已採納，改列「超過 `L1` 的動作」並註明擋它的理由不是「算互動」 |
| 3 `L1` 無 MCP 怎麼截圖 | ⬜ **仍開著**。我本輪 grep 全檔確認 `foundry-browser` 沒有任何一段給出 `L1` 的截圖做法，**是既有缺口、非本單造成**（§2 分級表在本單之前就寫著 `L1` ＝渲染＋截圖）。Tech Lead 不代開單的理由成立：新單缺 GitHub 鏡像會讓 `mirror-recon` 立刻紅、卡住全 workspace 的 commit（MYL-54 的已知觸發點），開單與建鏡像該一起做。**建議 Scrum Master 承接** |
| 4 加 `--selfcheck` 檢查散文 §N 引用 | ⬜ 仍開著，同意不在本單範圍 |
| 4.1 我第一輪指出交接留言 R2 為誤報 | ✅ Tech Lead 接受更正、未動檔案。處置正確——`CLAUDE.md`:45 的「三把鑰匙見 §5」指的是入口檔自己的 §5 |

**本輪新發現一項（次要，不擋結案，註明是新發現、不是 Tech Lead 沒修）**：
改寫後的理由 2 仍是一個 agent **實測不到**的宣稱——tool-profile 為 board-only，
打過去一律 403（known-drift `L7`），沒有人能驗證它的粒度真的是「工具」。
不要求本單修改，理由有二：一是這個措辭出自 AC2 本身（「粒度是工具不是等級」），
不是 Tech Lead 自行發明；二是理由 1（board-only、規則層改不動）已經獨立成立，
理由 2 只是「就算搆得到也解不了」的補強，錯了不影響結論。
若日後有人實際看到 tool-profile 的形狀，回頭在該處補一句證據等級即可。

## 5. 分支收尾檢查

- 分支狀態：**待合併**。本單交付物是規範定稿，依 T1／T2（MYL-73／MYL-74）的作法，
  APPROVED 後指回 CEO 合併——`roles/ceo`:54「不自行修改 protocol／角色 skill 定稿：
  提案 → 使用者核可 → 才由執行工單落檔」，合併者是 CEO 不是 Code Reviewer。
- 分支上只有本單的兩顆 commit（`e2902aa`、`d0464dc`）＋本審查報告，無夾帶。

### 合併者必讀

1. **走 protocol 第 7 節「手冊發佈審查」前四步**。本單的手冊 diff **不是戳記-only**——
   04 章新增一整節「卡片上的截圖，哪一種算證據」（11 行實質內容），
   **不走 `scripts/lib/publish-gate.sh:87` 的戳記旁路**，要寫新的 APPROVED 審查記錄。
2. **`handbook_commit` 填 `d0464dc4891f5c7a2f0975544fe51d6fcf68e597`**——我本輪自行覆驗過
   （`git log -1 --format=%H -- docs/handbook` 於 `d0464dc` 解析到這一顆）。
   ⚠️ 上一輪交接留言寫的 `e2902aa…` **已過期**（`d0464dc` 又動了 04 章）。
   本審查報告只落在 `docs/features/`，不會再改變這個值；合併後仍請自行覆驗一次。
3. 四步屬 **P2 常設授權**、無使用者介入點。**第五步（打 tag 發精裝站）不是結案條件**（`V1`），不要順手做。

## Verdict

**✅ APPROVED**
