# LLD：{模組／元件名稱}

> 低階設計文件。由 Tech Lead 撰寫，寫到「Developer 不需要再做設計決策就能動工」的程度。
> 一份 LLD 對應 HLD 裡的一個元件或一組緊密相關的元件。每個欄位下的引導文字填寫時整段刪除。

| 欄位 | 值 |
| --- | --- |
| 狀態 | 草稿 / 定稿 |
| 上游 HLD | {docs/features/<模組>/hld.md 的路徑} |
| 作者 | {agent 名稱} |
| 最後更新 | {YYYY-MM-DD} |

## 1. 範圍

（本份 LLD 覆蓋 HLD 的哪些元件、對應哪些 FR。一句話即可。）

## 2. 介面定義

（對外暴露的每個介面：API endpoint、函式簽名、CLI 指令或事件。含輸入、輸出、錯誤回應。）

### {介面名稱，例：POST /api/orders}

- **輸入**：（參數／body 欄位、型別、必填與否、驗證規則）
- **輸出**：（成功時的回應結構與範例）
- **錯誤**：（每種可預期錯誤的條件、狀態碼／錯誤型別、訊息）

## 3. 資料模型

（新增或變更的資料結構：表結構、schema、型別定義。欄位逐一寫明型別、約束、預設值。
變更既有結構時，額外寫遷移方式與相容性影響。）

## 4. 內部流程

（介面被呼叫後內部怎麼走：關鍵步驟、分支條件、與其他元件的呼叫順序。
只寫「有決策含量」的流程；一眼能懂的 CRUD 不用畫。）

## 5. 錯誤處理與邊界

（PRD 第 5 節的每個邊界情境，在這裡對應到具體的處理位置與策略：在哪一層擋、怎麼回報、要不要重試。）

## 6. 測試切入點

（給 Developer 與 QA 的提示：哪些邏輯適合單元測試、哪些要整合測試、有沒有需要 mock 的外部依賴。）

## 7. 實作注意事項

（不寫進程式碼會被踩雷的事：執行順序限制、並發考量、效能陷阱、既有程式碼的坑。沒有就寫「無」。）
